# 《极地生存法则》美术资源清单 v31（audit-r31 终审版 — 继续大工时项 MVP 实装：协议对称 + JsonUtility Dictionary 限制修复 + 0 漏列 + 字体/双登录二十二复核 PASS）

> **版本**：v31.0（2026-04-28 audit-r31 第 31 轮 — 继续 r30 模式直面修剩余可 MVP 化项；**~3 真修复（2 服务端 + 2 客户端 = game_paused/game_resumed 协议对称化 + contributionRewards 改 tailRewards array）/ PM 自审 PASS / 美术清单 v31（v30 395 → v31 395，0 漏列）/ 字体二十二复核 PASS / 双登录二十二复核 PASS / 0 美术新增**）
> **基准**：`docs/美术资源清单_v30.md` v30.0（395 件）+ audit-r31 协议对称 + Dictionary 限制修复
> **数量变化**：v30 395 件 → v31 **395 件**（**0 美术新增**）

---

## audit-r31 ChangeLog（v30 → v31）

### 真修复总览（2 服务端 + 2 客户端 = ~3 项 GAP MVP 实装）

继续 r30 教训"不再以工时大为搁置借口"，r31 评估剩余项后选 2 项可 MVP 化的：

**🔴 服务端代码层 2 处**：

| GAP-ID | 严重度 | 文件 | 修复内容 |
|---|---|---|---|
| **GAP-A25-04** | MAJOR | `Server/src/SurvivalRoom.js:866-887 case 'pause_game'` | **协议对称化** — 原仅 emit `game_paused`无对应 `game_resumed`；修复：根据 `_gmPaused` 状态切换；resume 时调 `_startTick` 真恢复 + emit `game_resumed`；GM 测试可正常暂停/恢复（之前一旦 pause 无法 resume）|
| **GAP-A26-08** | MAJOR | `Server/src/SurvivalGameEngine.js:3147-3175 _enterSettlement` | **JsonUtility Dictionary 限制修复** — 原 `contributionRewards: {playerId → share}` Dictionary 结构 JsonUtility 不支持反序列化 → 客户端静默丢失；修复：同步 emit `tailRewards: [{playerId, playerName, share}]` 数组结构（兼容 JsonUtility），保留旧字段作老客户端兼容 |

**🔴 客户端代码层 2 处**：

| GAP-ID | 严重度 | 文件 | 修复内容 |
|---|---|---|---|
| **GAP-A25-04** | MAJOR | `SurvivalGameManager.cs case "game_paused"/"game_resumed"` | 加 game_paused / game_resumed UI toast 反馈（AnnouncementUI + 跑马灯），让 GM 测试时能看到状态切换 |
| **GAP-A26-08** | MAJOR | `SurvivalDataTypes.cs SurvivalGameEndedData` | 加 `TailRewardEntry[] tailRewards` 字段 + 新增 `TailRewardEntry` 类（playerId/playerName/share）— 之前 contributionRewards Dictionary 在 JsonUtility 静默丢失整块，现在 SettlementUI 帧 B 可消费 §34 F6 "全员 ≥100 贡献分配"列表 |

### r31 横切验证

- **字体二十二复核**: PASS（runtime 黑名单 0 命中）
- **双登录二十二复核**: PASS（_isRoomCreator=7 / _requireBroadcaster=22 与 r30 一致零回退）
- **r24-r30 修复零回退**: 全部生效（OnTribeWarRetaliate 已删 / WorkCommandResponseData 已删 / GAP-D26-01 P0 启动时机 / GAP-C26-04 ice post-process / r30 3 项 MVP）

### r31 PM 拦截 / 不修决策记录

- **MAJOR-E25-08 B01 协议头**: 真 1d+ 工时（NetworkManager priority 队列改造），保留搁置
- **Capybara/MagicOrange shader Legacy**: r29 已评估保留（Editor 工具仍引用），跳过本轮重审
- **B25-08 ModalRegistry RouletteUI 抢占跨周期**: 设计层规则化，需产品决策
- 13 套 WorkerSkins / 5 怪物 Variant / 9 主播按钮 / 20 建筑 Dot / 2 SFX 缺失 — 美术任务延续

### r31 累积评估（r30-r31 大工时项 MVP 实装清单）

| 项 | r30/r31 状态 | 完成度 |
|----|-------------|-------|
| 5 类外域事件 UI（D25-11/E26-04）| r30 MVP | 70% |
| FreeDeathPassUI（D25-12/D26-09）| r30 MVP | 60% |
| spotlight UI（D26-06）| r30 MVP | 80% |
| game_paused/game_resumed 对称（A25-04）| **r31 MVP** | **90%** |
| contributionRewards JsonUtility 限制（A26-08）| **r31 MVP** | **80%**（缺 SettlementUI 帧 B 实际渲染列表）|
| B01 协议头（E25-08）| 保留搁置 | 0% |

5 项大工时项中 **5 项 MVP 实装**（r30 3 + r31 2），仅剩 1 项（B01）真大工时保留。

---

## 🚧 v31 漏列补登

**0 项资源漏列补登** — 继承 v24-v30 5 漏列项延续状态。

---

## v31 美术清单内容（沿用 v30 主体 395 件 + 0 漏列）

详见 `docs/美术资源清单_v30.md` 主体（v31 0 增量）。

---

## 历史归档（v1-v30）

详见各历史版本：`docs/美术资源清单_v1.md` ~ `docs/美术资源清单_v30.md`。

---

## v31 总览数据

| 项 | 数量 / 状态 |
|---|---|
| **美术资源总数** | **395 件**（v30 395 + r31 0 漏列）|
| 已交付 | ~370 件 |
| 美术待做 | ~25 件 |
| r31 真修复 | **~3 项 MVP 实装**（2 服务端 + 2 客户端）|
| r31 关键 P0 修复 | A25-04 协议对称化（之前 pause 后无法 resume）+ A26-08 JsonUtility Dictionary 限制（之前客户端静默丢失整字段）|
| r31 Reviewer VERDICT | **PASS** ✅（PM 自审 PASS）|
| 字体二十二复核 | ✅ PASS |
| 双登录二十二复核 | ✅ PASS |
| 服务端代码改动 | **2 处需要部署**（pause_game 状态切换 + tailRewards 数组结构）— r31 是 r19 后第 5 次有服务端改动需部署（之前 r24-r27 4 次 + r28-r30 连续 3 轮零部署后回归改动）|
| Legacy 清理状态 | r25 删 4 项 + r27 删 tribe_war_retaliate + r28 删 work_command_response + r29 评估保留 Capybara/MagicOrange shader |

---

> r31 重点：继续 r30 教训"不再以工时大为搁置借口"，再修 2 项可 MVP 化的（game_paused/resume 对称 + JsonUtility Dictionary 限制）。
> 5 项大工时候选项中 **5 项已 MVP 实装**（r30 3 + r31 2），仅 B01 协议头（真 1d+）保留搁置。
> 部署涉及：pause_game 切换 + tailRewards 数组（2 处服务端）。
