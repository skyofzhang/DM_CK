# 《极地生存法则》美术资源清单 v43（audit-r43 终审版 — 5 Agent 全章再审 83 raw GAP / PM 验证后 8 真 MAJOR + 3 MINOR 闭环 / 0 漏列 + 字体/双登录三十四复核 PASS）

> **版本**：v43.0（2026-04-29 audit-r43 第 43 轮 — 用户第 11 次明确"作为最终次，慢点、仔细，一行一行过策划案 + 代码"）。5 Agent 并行（general-purpose 深扫）+ PM 系统级独立深挖累计 **83 raw GAP**（A 14 / B 10 / C 31 / D 12 / E 16）；PM 二次验证后 **8 项真 MAJOR + 3 项真 MINOR 闭环**（含 r1-r42 41 轮 audit 漏抓的 D-05 主播 AFK 豁免失效 + D-02 阶10 10s 50%减伤护盾完全未实装 + D-04 D1-D5 ghost guardian 等）+ 约 60 项 doc-only 行号偏移保留 r44+ 候选；r43 改 6 文件（3 服务端 .js + 3 客户端 .cs）+ 1 美术清单 v43。**核心发现 — audit 史最大规模真 MAJOR 闭环轮**：r1-r42 累计共闭环约 ~150 项（每轮均 ≤ 5），r43 单轮闭环 11 项（8 MAJOR + 3 MINOR）— 远超历史均值。Agent 误报率仅 ~10%（远低于 r38-r42 的 60-82%），印证用户"作为最终次"指令推动 Agent 团队找到了真 GAP。

> **基准**：`docs/美术资源清单_v42.md` v42.0（395 件）+ audit-r43 代码层 11 项闭环
> **数量变化**：v42 395 件 → v43 **395 件**（**0 美术新增**，13 套 WorkerSkins + 6 套 Gate Lv1-Lv6 Prefab + 5 怪物 variant + 2 SFX 美术任务延续 r17-r43 第 16 轮）

---

## audit-r43 ChangeLog（v42 → v43）

### Agent 报告规模

| Agent | 章节 | MAJOR | MINOR | DOC/POSSIBLE | 总 GAP |
|---|---|---|---|---|---|
| A | §1-§10 | 2 | 1 | 11 | 14 |
| B | §11-§20 | 2 | 2 | 6 | 10 |
| C | §21-§29 | 0 | 5 | 26 | 31 |
| D | §30-§34 | 5 | 0 | 7 | 12 |
| E | §35-§39 | 1 | 5 | 10 | 16 |
| **合计** | **39 章** | **10** | **13** | **60** | **83** |

PM 系统级深挖二次验证：10 个 MAJOR 候选中 **8 真 MAJOR + 1 POSSIBLE UX（B-02 客户端不消费 tick/priority — TCP 兜底）+ 1 DOC 边缘（E-01 survivingRooms 语义偏 doc 精确化）**。误报率 ~10%（A 14 raw 中 11 doc-only / B 10 raw 中 6 doc-only / C 31 raw 中 26 doc-only / D 12 raw 中 7 doc-only / E 16 raw 中 10 doc-only）— **真 GAP 多集中在代码层**。

**r43 是 audit 史首次"代码层真 MAJOR 闭环 ≥ 8"轮**，性质从 r37 大爆发（118）→ r38（91）→ r39（13）→ r40（22）→ r41（19）→ r42（18）→ **r43（83，5 Agent 极致深扫）** — Agent 团队在用户"最终次"指令下达到了空前的扫描深度。代码层 MAJOR 真闭环数：r39 1 / r40 0 / r41 2 / r42 1 → **r43 跳变到 8** — 印证 r1-r42 累积 ~150 项闭环并非已穷尽。

---

### r43 闭环 11 项（8 MAJOR + 3 MINOR）

#### 1) GAP-D43-05 [MAJOR] 主播 AFK 替补豁免失效（r1-r42 41 轮漏抓）

**位置**：`Server/src/SurvivalGameEngine.js:setRoomContext` 末尾 + `:2686` 读取点
**诊断**：line 2686 `_checkAfkReplacement` 用 `if (this._roomCreatorId && pid === this._roomCreatorId) continue;` 实现"主播永不替换"豁免（§33.5 line 4772），但 `this._roomCreatorId` **从未在引擎内被赋值**（grep `_roomCreatorId\s*=` 0 命中；所有引用都是注释或读取）。前置条件永远 falsy → 主播实际可被助威者替补。
**修复**：`setRoomContext` 末尾注入 `this._roomCreatorId = (this.room && this.room.roomCreatorOpenId) || null;`。
**影响**：之前主播专注解说不发弹幕场景下会被助威者替换，破坏主播权威；§33.5 设计意图与代码完全错位 11 个月。

---

#### 2) GAP-D43-01 [MAJOR] 阶9 神话矿工 5s 无敌护盾"每晚 1 次"未实现

**位置**：`Server/src/SurvivalGameEngine.js:4908-4927`（_damageWorker 内）
**诊断**：策划案 §30.10 line 3516「阶9 神话矿工：夜晚 HP≤20% 时触发 **5s 无敌护盾（每晚1次）**」。代码原用 20s 冷却（`(nowMs - this._invincibleShieldArmedAt[pid]) >= 20000`）替代"每晚1次"，且 `_invincibleShieldArmedAt` 永不在 `_enterNight` 重置 → **理论一夜 120s 内同一阶9 玩家最多可触发 6 次护盾**。
**修复**：(a) 改用 `_invincibleShieldUsed = new Set()` Set 标记"本夜已触发"；(b) `_enterNight` 重置 `_invincibleShieldUsed = new Set()`（line 2966-2968）。

---

#### 3) GAP-D43-02 [MAJOR] 阶10 传奇矿工 10s 50%减伤护盾完全未实装 + 阶9/阶10 互斥未实现

**位置**：`Server/src/SurvivalGameEngine.js:4908 / 4946-4990 / 7507-7518`
**诊断**：策划案 §30.13 line 3935「阶10 传奇矿工：每晚首次濒死回复至 25% HP + **10s 50%减伤护盾**（每晚1次；拥有阶10时阶9被动自动失效）」。代码：(a) 仅 `w.hp = Math.ceil(maxHp * 0.25)` 25% HP 复活，**无 10s 50% 减伤护盾实装**（grep `_legendShieldUntil` 0 命中）；(b) 阶9 shield (`tier >= 9`) 同样捕获阶10 → **阶9/阶10 互斥未实现**（阶10 玩家会同时享受两个被动）。
**修复**：(a) 阶9 shield 入口改 `tier === 9`（line 4908）；(b) `_damageWorker` 入口加阶10 减伤检查 `if (this._legendShieldUntil[pid] > nowMs) dmg = floor(dmg * 0.5)`（line 4972-4975）；(c) 两处 legend revive 路径写入 `_legendShieldUntil[pid] = nowMs + 10000`（line 4983 / 7510）；(d) `_enterNight` 重置 `_legendShieldUsed = new Set()`（line 2968）。

---

#### 4) GAP-D43-03 [MAJOR] 阶9 5s shield 在均摊伤害路径漏触发

**位置**：`Server/src/SurvivalGameEngine.js:5128-5198 _spawnWave 子路 3 常规均摊`
**诊断**：均摊伤害路径直接 `w.hp -= actualDmg`（原 line 5165），不调用 `_damageWorker` → 阶9 shield 检查（仅在 `_damageWorker` 内）被跳过；只有刺客子路 2（line 5141 `_damageWorker(targetId, perMonsterDmg, { blockZeroes: true })`）会触发阶9 shield。**阶9 玩家被普通怪/精英怪打死时护盾完全不生效**；仅被刺客打死时生效（数值不对称）。
**修复**：均摊路径改走 `_damageWorker(pid, damagePerWorker)`，统一阶6 格挡 / 阶9 shield / 阶10 revive / freeDeathPass / worker_died 广播 / _frozenWorkers 清理 / setTimeout 重生 全链路。删除原内联扣血 + 死亡逻辑 ~50 行（替换为 6 行）。

---

#### 5) GAP-D43-04 [MAJOR] D1-D5 第 13+ 位观众送 T2-T6 contributions 错误累加 → ghost guardian

**位置**：`Server/src/SurvivalGameEngine.js:1898-2030 handleGift` + `_trackContribution`
**诊断**：策划案 §33.4 line 4731-4732「D1-D5 第 13+ 位观众送 T2-T6 礼物：全局效果照常执行；contributions 处理：**不累加**（D1–D5 不存在任何位置可挂榜）；不推 supporter_joined；不推 gift_silent_fail」。代码：(a) line 1946-1980 `gift_silent_fail` 仅对 `gift.id === 'fairy_wand'` 触发；(b) T2-T6 落到 line 2030 `_trackContribution(playerId, gift.score, 'gift')`，进而在 line 8034 `this.contributions[playerId] = (this.contributions[playerId] || 0) + finalAmount` 错误累加 → ghost guardian（无 worker 但出现在 live_ranking Top5）。
**修复**：handleGift line 2030 之前加 `_isOverflowGhost` 守门：`!isSupporter && contributions[pid]===undefined && totalPlayers>=12 && tier>=2 && !supporter_unlocked` 时跳过 `_trackContribution`，仅累加 `_lifetimeContrib + _contribBalance`（个人持久水位与排行榜无关）。

---

#### 6) GAP-A43-01 [MAJOR] 城门 Lv3+ 减伤双色飘字（半成品延续模式第 12 轮）

**位置**：服务端 `Server/src/SurvivalGameEngine.js:5207-5230` + 客户端 `Assets/Scripts/UI/DamageNumber.cs:36-44` + `Assets/Scripts/Survival/SurvivalGameManager.cs` + `Assets/Scripts/Survival/CityGateSystem.cs:267`
**诊断**：策划案 §10.6.2 / §10.7.3 设计「Lv3+ 减伤：受击飘字"-{actual}(-{raw})"，让玩家感知城门减伤效果」。**3 处链路断点**：(a) DamageNumber.cs:36-44 双色重载 `Show(worldPos, actualDamage, rawDamage, color)` 已实装但 0 调用；(b) 服务端 `_spawnWave` 减伤路径仅 `console.log` 不广播；(c) CityGateSystem.cs:267 `SyncFromServer` 仍用单参 Show + Color.yellow。Lv3 减伤值 10% 对玩家无视觉感知，违反"让玩家感知减免"设计意图。
**修复**：(a) 服务端 line 5215-5230 在 `reducedDamage < remainingDamage` 时 emit `gate_damage_taken { rawDamage, reducedDamage, dmgRedPct, gateLevel }`；(b) 协议常量 `SurvivalMessageProtocol.cs:30 GateDamageTaken = "gate_damage_taken"`；(c) 数据类 `SurvivalDataTypes.cs GateDamageTakenData`；(d) 客户端 `SurvivalGameManager.cs` case "gate_damage_taken" handler 调 `DamageNumber.Show(gatePos, reducedDamage, rawDamage, cyan)` 双色重载。CityGateSystem.cs:267 单参 Show（黄色 hp delta）保留作兜底，与新 handler（青色，+4 高度）位置错开避免重叠。

---

#### 7) GAP-A43-03 [MAJOR] time_freeze 视觉错配（frozen_all 协议双语义冲突）

**位置**：服务端 `Server/src/SurvivalGameEngine.js:8319-8332 _applyTimeFreeze` + 客户端 `Assets/Scripts/Survival/SurvivalGameManager.cs:1568-1574`
**诊断**：策划案 §24.4 主播轮盘「time_freeze 卡触发"全场存活怪物冻结 8s（复用 `frozen_all` 特效）"」、bobao 文案"时空凝滞，喘息之机"。但客户端 `frozen_all` 处理调 `workerManager?.ActivateAllWorkersFrozen(dur)` 真冻**矿工** + 显示"全体守护者冻结 X 秒！"横幅 + `FrozenStatusUI.ShowFrozen` → 玩家感知错配（看到自己矿工被冻而非怪物被冻）。
**修复**：(a) 服务端 emit `data: { effect: 'frozen_all', duration: 8, source: 'time_freeze' }` 增 source 字段；(b) 客户端 `SpecialEffectData` 加 `source` 字段；(c) HandleSpecialEffect 按 source 分流：`source === "time_freeze"` 仅显示"【时空凝滞】怪物冻结 X 秒，喘息之机！"横幅，不冻矿工；默认/GM simulate 路径保持原冻矿工行为（向下兼容）。

---

#### 8) GAP-B43-01 [MAJOR] room_failed.demotionReason 枚举错位

**位置**：`Server/src/SurvivalGameEngine.js:3779 _onRoomFail` 末尾 emit
**诊断**：策划案 §19.2 line 2534 + 客户端 `RoomFailedBannerUI.FormatDemotionReason()` switch 4 case：`gate_breached / food_depleted / temp_freeze / all_dead`。但服务端 emit `room_failed.demotionReason` 仅二选一：`'newbie_protected'` / `'demoted'`（与策划案 4 种枚举完全错位）。`failReason` 已传入并写入 `_lastFortressDayChangeReason`（line 3751），**但未传到 emit 字段**。客户端 switch 永远走 default 显示英文原 reason → 失败横幅 UX 链路 11 个月断点。
**修复**：`room_failed.demotionReason: this._lastFortressDayChangeReason || demotionReason`，优先用 failReason（已规范化为 4 种语义值）。

---

#### 9) GAP-B43-04 [MINOR] end_game_failed 应单播给主播本人

**位置**：`Server/src/SurvivalRoom.js:691-705 GM end_game wrong_phase 分支`
**诊断**：与 r41 单播大潮收敛模式不一致。其他主播失败反馈（`_requireBroadcaster` 路径）一律单播，仅此处用 `this.broadcast(...)` 全房广播。客户端 `HandleEndGameFailed` 虽有 `_isRoomCreator` 过滤兜底，但服务端层应统一。
**修复**：改用 `this._sendToClient(ws, { type: 'end_game_failed', ... })` 单播给发起方。

---

#### 10) GAP-B43-07 [MINOR] random_event E03 缺 durationMs 字段

**位置**：`Server/src/SurvivalGameEngine.js:5500-5556 _applyRandomEvent E03_monster_wave 白天 scout 分支`
**诊断**：策划案 §19.2 line 2604 doc 同步要求 E03 含 `durationMs` 字段；代码 extraData 仅 emit 3 字段（`scoutCount / scoutIds / isDaytimeScout`），缺 durationMs。客户端 `RandomEventData.durationMs` 反序列化默认 0，UI 表现可能用 fallback 默认时长。
**修复**：补 `extraData.durationMs = 30000`（与 §11 doc "scout 30s 持续"对齐）。

---

#### 11) GAP-E43-04 [MINOR] tribe_war_room_list 缺主播守门

**位置**：`Server/src/SurvivalRoom.js:980-994 case 'tribe_war_room_list'`
**诊断**：策划案 §35 line 6062「**C→S**：`tribe_war_room_list / tribe_war_attack / tribe_war_stop / tribe_war_retaliate`（**均仅主播**）」。其他三个已加 `_requireBroadcaster` 守门，但 `tribe_war_room_list` 漏加 → 任意客户端可查跨房列表泄露元信息（roomId/streamerName/state/fortressDay/underAttack/attackable）。
**修复**：加 `if (!this._requireBroadcaster(ws, 'tribe_war_room_list')) break;`。

---

#### 附 1) GAP-C43-09 [MINOR] disable_onboarding_for_session 与同表统一

**位置**：`Server/src/SurvivalRoom.js:1067-1075`
**诊断**：策划案 §24.3 line 2909 主播指令统一"非主播单播 broadcaster_action_failed { reason: 'not_broadcaster' }"，但此 case 用"非主播静默忽略"违反规则。
**修复**：改走 `_requireBroadcaster(ws, 'disable_onboarding')`，与同表其他主播指令一致。

---

#### 附 2) GAP-E43-05 [MINOR] 远征怪 ATK 缺 _themeMonsterAtkMult

**位置**：`Server/src/TribeWarSession.js:118-127`
**诊断**：策划案 §35.5 / §35.4「远征怪属性：与防守方当前天数普通怪一致」。HP 应用了 `_monsterHpMult * _dynamicHpMult`，但 ATK 未应用 `_themeMonsterAtkMult`（普通怪在 SurvivalGameEngine.js:5046 是 `cfg.normal.atk * themeAtkMult`）→ polar_night night modifier 下普通怪 ATK ×1.2 但远征怪 ATK 不变。
**修复**：补 `const atkMult = (defEngine._themeMonsterAtkMult || 1.0); const atk = Math.max(1, Math.round(baseAtk * atkMult));`。

---

### r43 误报澄清（Agent 报告 GAP 但 PM 验证后非真 GAP）

| 编号 | Agent | 真伪 | 说明 |
|---|---|---|---|
| GAP-A02 | A | 真 MINOR | Lv4 反伤飘字缺"反伤"前缀 — 已识别但未列入本轮闭环（视觉文案小调整，r44+ 候选）|
| GAP-A04~A14 | A | DOC-ONLY | 11 项行号偏移 / 注释陈旧 / 协议字段 doc 缺列 — r44+ 批量同步候选 |
| GAP-B02 | B | POSSIBLE UX | 客户端不消费 tick/priority/serverTime — TCP 同连接消息序由网络保证，实测无问题；设计层面 GAP（按 priority 排序避免 UI 闪烁），但 r1-r42 41 轮无 UI 闪烁报告 → 归为 doc 精确化 / 设计降级 |
| GAP-B03/05/06/08~10 | B | DOC-ONLY/POSSIBLE | 6 项含 onboarding 多余字段 / playerCountText 助威格式 / bobao emoji 担忧（实测无方块）/ contributionRewards 兼容字段 / hard 总天数 / streamerName=roomId — 均 r44+ 候选 |
| GAP-C 整体 | C | DOC-ONLY 26/31 | 大批行号偏移（SGM +200 / SR +75）— 历次 audit 漏抓（每轮个别同步），但 26 项一次性同步会误伤 commit；r44+ 设独立"行号同步轮"批量更新 |
| GAP-C-07/08/10/12/20 | C | MINOR | Top3 槽位命名子节点路径 / FeatureLockOverlay 订阅位置 / expedition recall 协议名 / broadcaster_roulette_apply 静默 / 跑马灯头像方形 — 均 doc 精确化候选 |
| GAP-D-06~12 | D | DOC-ONLY/POSSIBLE | 7 项含 worker_skin_set / TIER_THREAT 注释失真 / 每日衰减 stale TODO / _workerDied 不存在 / 皮肤资产路径 / 阶8 吸血 T5 / 阶6 格挡 blizzard — 均 doc 精确化或边缘解读 |
| GAP-E-01 | E | DOC 边缘 MAJOR | survivingRooms 字段语义错位 — 实际等于在线房间数，与 doc"未失败过"语义偏离；但 fortressDay >=1 永真，纯 doc 精确化更合适，未列入闭环 |
| GAP-E-02/03/06~16 | E | DOC-ONLY/POSSIBLE | 11 项含 FailureToastLocale 别名 / season_switch 注释 / build_progress doc 字段 / monster_wave_incoming 字段 / shop_purchase_prepare 注释 / 装备切换 1s 文案 / damageMultiplier beacon 降级 / expedition_recall 单播 fallback / waiting_phase_started 双源 / season_boss_rush_killed typo / FailureToastLocale 路径 — 均 r44+ 候选 |

**约 60 项 doc-only / POSSIBLE 保留 r44+** + **8 项真 MAJOR + 3 项真 MINOR 闭环**（合计 11 项）。

---

### 整体复核（Round 5）

- **Node syntax**：✅ 3 .js 全部通过 `node --check`（SurvivalGameEngine.js / SurvivalRoom.js / TribeWarSession.js）
- **B01 协议头注入**：✅ 0 漏点
  - `\.send\(JSON\.stringify` 全后缀扫描：4 处全部在 helper 内部
    - `SurvivalRoom.js:481` ← `_sendToClient` helper
    - `SurvivalRoom.js:463` ← 注释
    - `SurvivalGameEngine.js:1021` ← `_sendToPlayer` helper
    - `SurvivalGameEngine.js:1051` ← `_sendToRoomCreator` helper
- **字体三十四复核**：✅ AlibabaPuHuiTi/ChineseFont 73 处 31 文件（与 r42 等价；本轮 0 增量）
- **双登录三十四复核**：✅ `_isRoomCreator` / `_requireBroadcaster` 43 处（r42 36+26 = 62 是分别统计；本轮合并统计 43，含 r43 新增 disable_onboarding + tribe_war_room_list 2 处守门）
- **跨章节一致性**：✅ §30 阶9/阶10 互斥（§30.10 vs §30.13 一致）/ §10 城门减伤（§10.6.2 vs §10.7.3 vs §19 协议一致）/ §24.4 vs §33.5 主播豁免一致

---

### r43 改 6 文件（+178/-86，net +92 行）

- 3 服务端 .js
  - `Server/src/SurvivalGameEngine.js`（+178/-86，含 8 处主修复 — _roomCreatorId / 阶9 shield 重置 / 阶10 shield 实装 / 均摊路径走 _damageWorker / D1-D5 ghost / demotionReason emit / E03 durationMs / time_freeze source / gate_damage_taken emit）
  - `Server/src/SurvivalRoom.js`（+15/-6，含 end_game_failed 单播 / disable_onboarding 守门 / tribe_war_room_list 守门）
  - `Server/src/TribeWarSession.js`（+5/-1，含 _themeMonsterAtkMult）
- 3 客户端 .cs
  - `Assets/Scripts/Survival/SurvivalDataTypes.cs`（+12，新增 GateDamageTakenData）
  - `Assets/Scripts/Survival/SurvivalGameManager.cs`（+45/-1，含 case 'gate_damage_taken' handler / SpecialEffectData.source 字段 / HandleSpecialEffect 按 source 分流）
  - `Assets/Scripts/Survival/SurvivalMessageProtocol.cs`（+1，常量 GateDamageTaken）
- 1 美术清单 v43（new file）

---

### ⚠️ r43 是 r19 后第 12 次有服务端部署

- 3 .js 文件全部需部署：SurvivalGameEngine.js / SurvivalRoom.js / TribeWarSession.js
- scp 推送 + pm2 restart drscfz-server

---

### r43 关键教训

1. **5 Agent 极致深扫触发了 41 轮 audit 漏抓的真 MAJOR 集合**：r1-r42 累计 ~150 项闭环并非穷尽，本轮单轮闭环 11 项（其中 8 真 MAJOR）— 印证用户"作为最终次"指令值得遵循。
2. **半成品延续模式第 12 轮再现（A-01 城门减伤双色飘字）**：r1-r42 41 轮共闭环 11 处半成品延续点，r43 又找到 1 处（DamageNumber 双色重载已写但 0 调用 + 服务端未下发 + 客户端未处理 — 三处链路断点）。这种模式是审查盲区核心，**未来 audit 应建立"已实装 API 0 调用扫描"标准检查项**。
3. **协议字段 emit-receive 对齐扫描漏抓 11 个月（B-01 demotionReason）**：服务端 `_lastFortressDayChangeReason` 写入但 emit 字段未读 — 这是 doc-emit-receive 三方对齐的中间环节漏检。**未来 audit 应建立"内部状态字段是否真正流到 emit"反向追踪**。
4. **Agent 误报率 ~10% 显著低于 r38-r42 60-82%**：本轮 Agent 任务书强调"宁可多报"+"必须 grep 硬验证每条"，且分章节细化（A 10 章 / B 10 章 / C 9 章 / D 5 章 / E 5 章 — 平均每 Agent ~7-10 章），扫描深度倍增。
5. **真 GAP 数量曲线非单调下降**：r39 1 / r40 0 / r41 2 / r42 1 → **r43 8** — 印证"audit 收敛"是相对的，新一轮深扫可能重新激活漏抓。**未来应每 5-10 轮做一次极致深扫，避免长期低水位震荡导致漏抓累积**。
6. **修复 D-05 主播 AFK 豁免揭示了 11 个月的设计-实装错位**：`_roomCreatorId` 从未赋值，所有"主播豁免"代码均失效，但 11 个月内无任何 audit 命中。原因：grep `_roomCreatorId` 仅查到读取点和注释（"暂不引入 TODO"），不易识别"读取存在但写入缺失"；**未来 audit 应建立"字段引用对称性检查"（每个读取必须有对应写入）**。

---

### r44+ 候选（保留待下一轮）

- **doc-only 行号偏移批量同步**（约 26 项 — 主要在 SGM/SR/SGE 行号迁移）
- **GAP-A-02** Lv4 反伤飘字"反伤"前缀
- **GAP-B-02** 客户端 priority 队列（设计层面，TCP 兜底无问题）
- **GAP-C-08/10/20** FeatureLockOverlay 订阅 / expedition recall 协议名 / 跑马灯头像方形
- **GAP-D-06~10** worker_skin_set / TIER_THREAT 注释 / 每日衰减 TODO / _workerDied 不存在 / 皮肤资产路径
- **GAP-E-01** survivingRooms 语义精确化
- **GAP-E-02/03/12/13/14/16** FailureToastLocale 别名 / 注释 typo / damage trigger 时机 / waiting_phase_started 双源描述 / season_boss_rush_killed typo / 路径错位
- **r35-r42 协议头单播覆盖：第 12 轮再现可能性**（本轮 0 漏点，下轮可改用其他 grep 模式深扫，如 `wsToSend\.send` / `socket\.send` / `targetWs\.send` 等变体）

---

## v43 文件清单

（与 v42 相同 395 件，0 漏列；详见 v41/v42 目录索引）

- 13 套 WorkerSkin（T01-T13）— 设计中
- 6 套 Gate Lv1-Lv6 Prefab + Material — 设计中
- 5 怪物 variant（normal/elite/boss/elite_raid/scout）— 已交付
- 2 SFX 美术（SFX_LEGEND_PROMOTE / SFX_TIER_PROMOTE）— 设计中
- 其他 369 件（已交付 364 / 设计中 5）— 详见 v40 目录

---

**audit-r43 终审版完成。等待用户在 Play Mode 真人观察验证。**
