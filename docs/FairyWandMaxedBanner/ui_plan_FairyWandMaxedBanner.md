# FairyWandMaxedBanner UI 策划案

> Prefab：`Assets/Prefabs/UI/Panels/FairyWandMaxedBanner.prefab`  
> 用途：仙女棒满额闪光跑马灯  
> 主效果图规格：1080 x 1920 竖屏  
> 日期：2026-05-07

## 1. 界面定位与目的

在仙女棒累计达到阈值时给出全屏闪光与横向播报。

该界面应被视为 仙女棒满额闪光跑马灯，不是营销页。设计第一屏必须直接表达真实可用状态，并保留足够游戏主体安全区或明确的弹窗遮罩关系。

## 2. 设计目标

- 闪光不致盲
- 跑马灯文字足够大
- 短时反馈不残留

## 3. 当前 prefab / 模块核对

| 项目 | 内容 |
| --- | --- |
| Prefab 路径 | `Assets/Prefabs/UI/Panels/FairyWandMaxedBanner.prefab` |
| 绑定脚本 | `FairyWandMaxedBanner` |
| 节点数量摘录 | 3 个 `m_Name` 记录 |
| 文案数量摘录 | 0 个 TMP/Text 文案记录 |
| 嵌套子 prefab | 无直接嵌套 Panels prefab |

关键节点摘录：

- MarqueeRoot
- FlashRoot
- MarqueeText

可见/默认文案摘录：

- 当前 prefab 文案多由运行时注入，设计稿应预留动态文本宽度。



## 4. 推荐布局与层级

FlashRoot 做瞬时闪白，MarqueeRoot/MarqueeText 横向通过。

| 模块 | Prefab/节点 | x | y | w | h | 说明 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| 全屏闪光 | `FlashRoot` | 0 | 0 | 1080 | 1920 | 0.5 秒 |
| 主画面安全区 | `SceneSafeArea` | 55 | 260 | 970 | 1160 | 闪光后恢复 |
| 跑马灯轨道 | `MarqueeRoot` | 0 | 840 | 1080 | 120 | 右进左出 |
| 跑马灯文案 | `MarqueeText` | 120 | 870 | 840 | 58 | 仙女棒能量已满 |

## 5. 模块设计说明

### 全屏闪光

- 对应节点：`FlashRoot`
- 建议位置：x=0 y=0 w=1080 h=1920。
- 设计要点：0.5 秒

### 主画面安全区

- 对应节点：`SceneSafeArea`
- 建议位置：x=55 y=260 w=970 h=1160。
- 设计要点：闪光后恢复

### 跑马灯轨道

- 对应节点：`MarqueeRoot`
- 建议位置：x=0 y=840 w=1080 h=120。
- 设计要点：右进左出

### 跑马灯文案

- 对应节点：`MarqueeText`
- 建议位置：x=120 y=870 w=840 h=58。
- 设计要点：仙女棒能量已满

## 6. 视觉风格

星尘金与冰蓝光效，闪光只半秒，跑马灯 3 秒内结束。

建议保持项目统一语言：冰蓝代表常规 HUD，金色代表贡献/VIP/奖励，红橙代表危险或不可逆操作，绿色代表修复/完成。所有文字必须在 1080x1920 竖屏下可读，按钮文字不得溢出。

## 7. AI 效果图场景

屏幕短暂闪亮，金色文字从右向左经过，提示奖励或效果触发。

效果图应把施工原型里的坐标和灰盒理解为布局约束，而不是最终视觉元素。最终图可以加入材质、光效、图标、进度条和真实游戏背景，但不要出现 prefab 名、坐标、虚线框、施工标注等文字。

## 8. 实现注意事项

- 当前 prefab 没有额外特殊实现项；按以下通用规则落地。
- 所有临时层应通过 CanvasGroup 或子节点显隐控制，避免长期遮挡中央 gameplay 区。
- 动态文本需要预留 20%-30% 的宽度余量，中文、数字和昵称都要能截断或滚动。
- 若该界面作为其他主面板的子面板复用，应优先服从主面板坐标，不再另起一套视觉规范。

## 9. 验收标准

- 文件均位于 `docs/FairyWandMaxedBanner/`。
- SVG 和 PNG 都是 1080x1920 竖屏参考。
- 效果图能清楚表达 仙女棒满额闪光跑马灯 的常用状态。
- 默认文案、节点名称和脚本关系能在文档中追溯到 prefab。
- 若存在嵌套子 prefab，README 和批量索引中明确避免重复生成。