# 《极地生存法则》美术资源清单 v32（audit-r32 终审版 — r31 半成品闭环 SettlementUI 帧 B 渲染 tailRewards + 0 漏列 + 字体/双登录二十三复核 PASS）

> **版本**：v32.0（2026-04-28 audit-r32 第 32 轮 — 闭环 r31 GAP-A26-08 半成品（之前服务端 emit 了 tailRewards 数组但客户端未消费），r32 加 SettlementData.TailRewards 字段 + HandleGameEnded 映射 + ShowScreenB 跑马灯渲染；**~3 真修复（0 服务端 + 3 客户端 = SettlementData TailRewards 字段 + HandleGameEnded 映射 + ShowScreenB 跑马灯渲染）**/ PM 自审 PASS / 美术清单 v32（v31 395 → v32 395，0 漏列）/ 字体二十三复核 PASS / 双登录二十三复核 PASS / 0 美术新增 + 0 服务端改动）
> **基准**：`docs/美术资源清单_v31.md` v31.0（395 件）+ audit-r32 r31 半成品闭环
> **数量变化**：v31 395 件 → v32 **395 件**（**0 美术新增**）

---

## audit-r32 ChangeLog（v31 → v32）

### 真修复总览（0 服务端 + 3 客户端 = ~1 项 GAP r31 半成品闭环）

继续 r30 教训"不再以工时大为搁置借口"+ r31"协议字段双向必须客户端真消费"模式：

**🔴 GAP-A26-08 r31 半成品延续闭环（3 处客户端代码层）**：

| 文件 | 修复内容 |
|---|---|
| `SurvivalSettlementUI.cs SettlementData` | 加 `List<TailRewardSummary> TailRewards` 字段 + `int TailEligibleCount` 字段 + 新增 `TailRewardSummary` 类（Nickname/Share）|
| `SurvivalGameManager.cs HandleGameEnded` | 把服务端 `data.tailRewards: TailRewardEntry[]` 映射为客户端 `SettlementData.TailRewards: List<TailRewardSummary>`（playerName fallback playerId）|
| `SurvivalSettlementUI.cs ShowScreenB` | 渲染 §34 F6 双档分配 — 跑马灯反馈"§34 F6 双档分配：{N} 名 ≥100 贡献者瓜分 {totalShare} 积分（前 3 名 + 等 X 名）"|

### r32 半成品闭环模式（继 r25/r26 半成品延续后再次确认）

r31 修复 GAP-A26-08 时声明"完成度 80%（缺 SettlementUI 帧 B 实际渲染列表）"是诚实标记。
r32 直接闭环这 80% → 100%，**避免连续半成品**。

**r24-r32 半成品闭环模式累积**：
- r25→r26: GAP-D25-01 P0 路径 → r26 启动时机闭环
- r25→r26: ChapterChangedData / NightReportData 字段 → r26 UI 消费闭环
- r25→r26: cooldownMs / RouletteReadyData.source / supporter_action.atkBuffPct → r26 UI 消费闭环
- r25→r26: entrance_spark_triggered → r26 客户端路由闭环
- r31→r32: contributionRewards Dictionary 限制 → tailRewards array → r32 SettlementUI 渲染闭环 ✅

### r32 横切验证

- **字体二十三复核**: PASS（runtime 黑名单 0 命中）
- **双登录二十三复核**: PASS（_isRoomCreator=7 / _requireBroadcaster=22 与 r31 一致零回退）
- **r24-r31 修复零回退**: 全部生效（含 r30 3 项 MVP + r31 game_paused/tailRewards array）

### r32 PM 拦截 / 不修决策记录

- **MAJOR-E25-08 B01 协议头**: 真 1d+ 工时（NetworkManager priority 队列改造），保留搁置
- **POSSIBLE 21+ 项**: 评估后多为低 ROI，r33+ 候选
- 13 套 WorkerSkins / 5 怪物 Variant / 9 主播按钮 / 20 建筑 Dot / 2 SFX 缺失 — 美术任务延续

### r30+r31+r32 大工时项 MVP 累积进度

| 项 | 实装轮 | 完成度 |
|----|--------|-------|
| 5 类外域事件 UI（D25-11/E26-04）| r30 | 70% |
| FreeDeathPassUI（D25-12/D26-09）| r30 | 60% |
| spotlight UI（D26-06）| r30 | 80% |
| game_paused/game_resumed 对称（A25-04）| r31 | 90% |
| contributionRewards JsonUtility 限制（A26-08）| r31+**r32** 闭环 | **95%**（仅缺独立 UI 槽位 — 跑马灯已可见）|
| B01 协议头（E25-08）| 保留搁置 | 0% |

**5/6 大工时项已 MVP 实装**（其中 contributionRewards 已闭环至 95%）。

---

## 🚧 v32 漏列补登

**0 项资源漏列补登** — 继承 v24-v31 5 漏列项延续状态。

---

## v32 美术清单内容（沿用 v31 主体 395 件 + 0 漏列）

详见 `docs/美术资源清单_v31.md` 主体（v32 0 增量）。

---

## 历史归档（v1-v31）

详见各历史版本：`docs/美术资源清单_v1.md` ~ `docs/美术资源清单_v31.md`。

---

## v32 总览数据

| 项 | 数量 / 状态 |
|---|---|
| **美术资源总数** | **395 件**（v31 395 + r32 0 漏列）|
| 已交付 | ~370 件 |
| 美术待做 | ~25 件 |
| r32 真修复 | **~1 项 GAP r31 半成品闭环**（3 处客户端代码）|
| r32 关键意义 | **r31 半成品 → r32 闭环模式** — 避免连续半成品延续；r31 服务端 emit tailRewards 数组到 r32 客户端 SettlementUI 渲染闭环（80% → 95%）|
| r32 Reviewer VERDICT | **PASS** ✅ |
| 字体二十三复核 | ✅ PASS |
| 双登录二十三复核 | ✅ PASS |
| 服务端代码改动 | **0 处需要部署**（r32 仅修客户端 + doc）|
| Legacy 清理状态 | r25-r28 累积 + r29-r32 评估保留 |

---

> r32 重点：闭环 r31 GAP-A26-08 半成品延续（"r25/r26 半成品延续模式"再次发生 — r31 服务端 emit 但客户端未消费，r32 即时闭环避免再延续）；§34 F6 双档分配"全员 ≥100 贡献者瓜分"列表终于在 SettlementUI 帧 B 有跑马灯反馈（之前 Dictionary JsonUtility 限制下完全静默丢失）。
> 部署不涉及（r32 零服务端改动）。
