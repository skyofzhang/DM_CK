# ReconnectDialog UI 策划案

> Prefab：`Assets/Prefabs/UI/Panels/ReconnectDialog.prefab`  
> 用途：断线重连确认弹窗  
> 主效果图规格：1080 x 1920 竖屏  
> 日期：2026-05-07

## 1. 界面定位与目的

检测到未完成游戏时让用户继续上一局或放弃重开。

该界面应被视为 断线重连确认弹窗，不是营销页。设计第一屏必须直接表达真实可用状态，并保留足够游戏主体安全区或明确的弹窗遮罩关系。

## 2. 设计目标

- 主操作“继续上一局”优先
- 风险文案明确
- 按钮间距避免误触

## 3. 当前 prefab / 模块核对

| 项目 | 内容 |
| --- | --- |
| Prefab 路径 | `Assets/Prefabs/UI/Panels/ReconnectDialog.prefab` |
| 绑定脚本 | `ReconnectDialog` |
| 节点数量摘录 | 8 个 `m_Name` 记录 |
| 文案数量摘录 | 4 个 TMP/Text 文案记录 |
| 嵌套子 prefab | 无直接嵌套 Panels prefab |

关键节点摘录：

- NewGameButton
- DescText
- TitleText
- Label
- DialogBox
- ReconnectButton
- ButtonRow

可见/默认文案摘录：

- `发现未完成的游戏，是否继续上一局？`
- `检测到上一局进行中`
- `继续上一局`
- `放弃，重新开始`



## 4. 推荐布局与层级

DialogBox 承载 TitleText、DescText、ReconnectButton、NewGameButton。

| 模块 | Prefab/节点 | x | y | w | h | 说明 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| 暗化背景 | `ModalBackdrop` | 0 | 0 | 1080 | 1920 | 重连弹窗 |
| 弹窗盒 | `DialogBox` | 150 | 560 | 780 | 540 | 重连确认 |
| 标题 | `TitleText` | 210 | 625 | 660 | 70 | 检测到上一局进行中 |
| 说明 | `DescText` | 220 | 730 | 640 | 120 | 是否继续上一局？ |
| 按钮行 | `ReconnectButton/NewGameButton` | 210 | 920 | 660 | 96 | 继续 / 放弃重开 |

## 5. 模块设计说明

### 暗化背景

- 对应节点：`ModalBackdrop`
- 建议位置：x=0 y=0 w=1080 h=1920。
- 设计要点：重连弹窗

### 弹窗盒

- 对应节点：`DialogBox`
- 建议位置：x=150 y=560 w=780 h=540。
- 设计要点：重连确认

### 标题

- 对应节点：`TitleText`
- 建议位置：x=210 y=625 w=660 h=70。
- 设计要点：检测到上一局进行中

### 说明

- 对应节点：`DescText`
- 建议位置：x=220 y=730 w=640 h=120。
- 设计要点：是否继续上一局？

### 按钮行

- 对应节点：`ReconnectButton/NewGameButton`
- 建议位置：x=210 y=920 w=660 h=96。
- 设计要点：继续 / 放弃重开

## 6. 视觉风格

明确选择型弹窗，继续按钮高亮，放弃按钮低强调但可读。

建议保持项目统一语言：冰蓝代表常规 HUD，金色代表贡献/VIP/奖励，红橙代表危险或不可逆操作，绿色代表修复/完成。所有文字必须在 1080x1920 竖屏下可读，按钮文字不得溢出。

## 7. AI 效果图场景

暗化背景中央出现重连确认，两个按钮横向并列。

效果图应把施工原型里的坐标和灰盒理解为布局约束，而不是最终视觉元素。最终图可以加入材质、光效、图标、进度条和真实游戏背景，但不要出现 prefab 名、坐标、虚线框、施工标注等文字。

## 8. 实现注意事项

- 当前 prefab 没有额外特殊实现项；按以下通用规则落地。
- 所有临时层应通过 CanvasGroup 或子节点显隐控制，避免长期遮挡中央 gameplay 区。
- 动态文本需要预留 20%-30% 的宽度余量，中文、数字和昵称都要能截断或滚动。
- 若该界面作为其他主面板的子面板复用，应优先服从主面板坐标，不再另起一套视觉规范。

## 9. 验收标准

- 文件均位于 `docs/ReconnectDialog/`。
- SVG 和 PNG 都是 1080x1920 竖屏参考。
- 效果图能清楚表达 断线重连确认弹窗 的常用状态。
- 默认文案、节点名称和脚本关系能在文档中追溯到 prefab。
- 若存在嵌套子 prefab，README 和批量索引中明确避免重复生成。